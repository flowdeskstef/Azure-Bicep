RG-AVD:
Host Pool + Workspace + Session Host
Scaling Plan
Image Template
Image Gallery + Gallery Version
Managed Identity
Key Vault

RG-INFRA:
VNet + Subnets
NSG’s
NAT Gateway
Load Balancer

RG-COMPUTE:
Domain Controller VM
App Server VM + Data Disk

RG-STORAGE:
2 Storage Accounts + File Shares
Backup Vault (incl. VM backups, file share backups)

RG-Monitoring:
Log Analytics Workspace

avd resources:
1 host pool, 1 workspace and 1 sessionhost + scaling plan + image template
2 storage accounts + 2 private endpoints
1 dc server + 1 app server with data disk
1 load balancer
1 nat gateway
1 network + 3 subnets + 3 ngs's
1 keyvault
1 managed identity
1 automation acccount + 5 runbooks + private endpoint
1 backup vault (2 vm backups, 3 file shares) + private endpoint
1 image gallery + gallery version
1 private dns zone
1 Log Analytics Workspace